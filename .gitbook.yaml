# GitBook Configuration
root: ./content

structure:
  readme: README.md
  summary: SUMMARY.md

# Redirects (add as needed)
redirects: {}

# PDF export settings
# Note: PDF export must be enabled in GitBook dashboard settings

# Branding Configuration
# Note: Most branding (logo, colors, themes) is configured through the GitBook dashboard:
# Dashboard → Space Settings → Appearance

# GitBook metadata (useful for SEO and branding)
metadata:
  title: "DeFi University - Money Markets Course"
  description: "Master professional DeFi money markets, lending protocols, risk management, and yield optimization strategies"

# Links (configure in dashboard for header/footer)
# These are configured in: Dashboard → Space Settings → Appearance → Header & Footer

# Variables for consistent branding
variables:
  university_name: "DeFi University"
  course_name: "Money Markets Course"

